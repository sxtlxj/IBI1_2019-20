First find whether the number is between 1 and 23：N (N is length of  numbers input
Then use recursion to find all permutations and merge them to one number at last: N(first for loop)*N(next for loop)*N(recursion time)
complexity = N+N**3=O(n**3)

